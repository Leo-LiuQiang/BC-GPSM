% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmatch_fit_ps_fold.R
\name{pmatch_fit_ps_fold}
\alias{pmatch_fit_ps_fold}
\title{Fit PS model on training fold and predict probabilities on eval fold}
\usage{
pmatch_fit_ps_fold(
  train_df,
  eval_df,
  t_name,
  x_names,
  model = c("logit", "gbm", "gam", "xgboost", "ranger"),
  tune = FALSE,
  tune_control = NULL,
  tune_grid = NULL,
  gps_params = NULL,
  tr_levels = NULL,
  seed = 12345
)
}
\arguments{
\item{train_df}{training data frame}

\item{eval_df}{evaluation data frame}

\item{t_name}{treatment column name (factor)}

\item{x_names}{covariate column names}

\item{model}{"logit","gbm","gam","xgboost","ranger". Flexible learners
require optional packages; XGBoost and ranger are experimental.}

\item{tune}{Logical; optional caret tuning for logit/gbm.}

\item{tune_control}{\code{caret::trainControl()} or NULL.}

\item{tune_grid}{named list with components logit/gbm (same style as your gps_tune_grids)}

\item{gps_params}{optional named list of model params (same style as your gps_params)}

\item{tr_levels}{full set of treatment levels to enforce (character)}

\item{seed}{integer seed}
}
\value{
list(e_hat_eval = n_eval x K matrix, levels = tr_levels)
}
\description{
Fit PS model on training fold and predict probabilities on eval fold
}
\examples{
set.seed(1)
n <- 75
dat <- data.frame(
  trt = factor(rep(c("A", "B", "C"), each = 25)),
  x1 = rnorm(n),
  x2 = runif(n)
)

train_id <- c(1:20, 26:45, 51:70)
ps_fit <- pmatch_fit_ps_fold(
  train_df = dat[train_id, ],
  eval_df = dat[-train_id, ],
  t_name = "trt",
  x_names = c("x1", "x2"),
  model = "logit",
  tr_levels = levels(dat$trt)
)

head(ps_fit$e_hat_eval)
}
