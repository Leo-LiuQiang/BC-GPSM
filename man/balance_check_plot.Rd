% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance_check_plot.R
\name{balance_check_plot}
\alias{balance_check_plot}
\title{Covariate balance check plot}
\usage{
balance_check_plot(
  data,
  treatment,
  covariate,
  treatment_ref = NULL,
  match_on = c("gps", "covariates"),
  gps_model = c("logit", "gbm", "gam", "xgboost", "ranger"),
  gps_params = NULL,
  fit_gps = TRUE,
  cov_distance = c("mahalanobis", "euclidean"),
  standardize = FALSE,
  ridge = 1e-08,
  match_ratio = 1L,
  threshold = 0.1,
  style = c("love", "faceted"),
  return_data = FALSE
)
}
\arguments{
\item{data}{Data frame containing treatment and covariates.}

\item{treatment}{Single numeric column index for treatment.}

\item{covariate}{Numeric vector of covariate column indices to check.}

\item{treatment_ref}{Optional reference treatment level. If \code{NULL}, the
last observed treatment level is used when a GPS fit is needed.}

\item{match_on}{Matching scale: \code{"gps"} or \code{"covariates"}.}

\item{gps_model}{GPS model used when \code{match_on = "gps"} and
\code{fit_gps = TRUE}; one of \code{"logit"}, \code{"gbm"},
\code{"gam"}, \code{"xgboost"}, or \code{"ranger"}. Flexible learners
require optional packages; XGBoost and ranger are experimental.}

\item{gps_params}{Optional named list of model-specific GPS parameters.}

\item{fit_gps}{Logical. If \code{TRUE} and \code{match_on = "gps"}, fit a
full-sample diagnostic GPS model before computing balance. If \code{FALSE},
\code{data} must already contain finite \code{loggps_*} columns.}

\item{cov_distance}{For covariate matching: \code{"mahalanobis"} or
\code{"euclidean"}.}

\item{standardize}{If \code{TRUE}, center and scale covariate matching
features before matching.}

\item{ridge}{Small ridge used for Mahalanobis whitening.}

\item{match_ratio}{Integer >= 1; number of nearest neighbors per target arm.}

\item{threshold}{Reference line for acceptable absolute SMD.}

\item{style}{Plot style. \code{"love"} aggregates across pairwise treatment
contrasts by taking the maximum absolute SMD for each covariate and sample;
\code{"faceted"} shows each pairwise treatment contrast separately.}

\item{return_data}{If \code{TRUE}, return a list with \code{plot} and
\code{balance}; otherwise return only the plot.}
}
\value{
A \pkg{ggplot2} object, or a list with the plot and balance table
when \code{return_data = TRUE}.
}
\description{
Computes pairwise absolute standardized mean differences (SMDs) before and
after nearest-neighbor matching, then returns a Love-plot style balance plot.
Matching can be performed on existing \code{loggps_*} columns, on a
full-sample diagnostic GPS fit, or directly on covariates.
}
\examples{
set.seed(1)
n <- 75
z1 <- rnorm(n)
z2 <- runif(n, -1, 1)
dat <- data.frame(
  trt = factor(rep(c("A", "B", "C"), each = 25)),
  age = 50 + 10 * z1,
  biomarker = z2,
  severity = 0.5 * z1 - 0.3 * z2 + rnorm(n, sd = 0.7),
  prior_tx = rbinom(n, 1, plogis(0.4 * z1)),
  site = factor(sample(c("S1", "S2"), n, replace = TRUE))
)

p <- balance_check_plot(
  data = dat,
  treatment = 1,
  covariate = 2:6,
  match_on = "covariates",
  style = "love"
)

p
}
